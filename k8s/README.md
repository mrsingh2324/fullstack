# Note: This k8s directory is optional
# Use terraform/ for MIG-based deployment instead

# If you prefer Kubernetes deployment:
# 1. Enable GKE: gcloud container clusters create mern-cluster
# 2. Update image paths in deployments
# 3. kubectl apply -f all.yaml
